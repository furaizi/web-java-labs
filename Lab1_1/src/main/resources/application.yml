spring:
  application:
    name: Lab1_2
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yml
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

springdoc:
  swagger-ui:
    url: /api-specs/cosmo-cats-product-api.yml

feature:
  cosmo-cats:
    enabled: true
  kitty-products:
    enabled: true
